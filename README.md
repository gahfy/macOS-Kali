# macOS-Kali

The purpose of this project is to install Kali tools on macOS.

For now, no maintainance at all.

Needs `xcode-select --install` and to install XCode (and accept all T&C)

To install, simply run:

```
./00-install.sh
```