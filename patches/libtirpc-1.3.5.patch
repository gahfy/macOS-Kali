diff -ruN a/src/Makefile.in b/src/Makefile.in
--- a/src/Makefile.in	2024-07-23 22:19:25
+++ b/src/Makefile.in	2024-08-30 23:15:01
@@ -90,7 +90,7 @@
 build_triplet = @build@
 host_triplet = @host@
 @AUTHDES_TRUE@am__append_1 = auth_des.c  authdes_prot.c  des_crypt.c  des_impl.c  des_soft.c  svc_auth_des.c
-@SYMVERS_TRUE@am__append_2 = -Wl,--version-script=$(srcdir)/libtirpc.map
+@SYMVERS_TRUE@am__append_2 =
 @GSS_TRUE@am__append_3 = auth_gss.c authgss_prot.c svc_auth_gss.c \
 @GSS_TRUE@			   rpc_gss_utils.c
 
diff -ruN a/src/getpeereid.c b/src/getpeereid.c
--- a/src/getpeereid.c	2024-07-22 13:10:59
+++ b/src/getpeereid.c	2024-08-30 23:25:02
@@ -28,13 +28,24 @@
 #include <sys/param.h>
 #include <sys/socket.h>
 #include <sys/un.h>
-
+#ifdef __APPLE__
+  #include <sys/types.h>
+#endif
 #include <errno.h>
 #include <unistd.h>
 
 int
 getpeereid(int s, uid_t *euid, gid_t *egid)
 {
+        #ifdef __APPLE__
+        int error;
+        error = getpeereid(s, euid, egid);
+        if (error != 0) {
+            return errno; // retourne le code d'erreur d'errno en cas d'échec
+        }
+    
+        return 0;
+        #else
 	struct ucred uc;
 	socklen_t uclen;
 	int error;
@@ -48,4 +59,5 @@
 	*euid = uc.uid;
 	*egid = uc.gid;
 	return (0);
+        #endif
  }
diff -ruN a/src/svc_vc.c b/src/svc_vc.c
--- a/src/svc_vc.c	2024-07-22 13:10:59
+++ b/src/svc_vc.c	2024-08-30 23:15:01
@@ -795,6 +795,9 @@
  */
 int
 __rpc_get_local_uid(SVCXPRT *transp, uid_t *uid) {
+#ifdef __APPLE__
+    return (-1);
+#else
 	int sock, ret;
 	gid_t egid;
 	uid_t euid;
@@ -809,6 +812,7 @@
 		return (ret);
 	} else
 		return (-1);
+#endif
 }
 
 /*
diff -ruN a/src/xdr_float.c b/src/xdr_float.c
--- a/src/xdr_float.c	2024-07-22 13:10:59
+++ b/src/xdr_float.c	2024-08-30 23:15:01
@@ -83,7 +83,11 @@
 };
 #else
 
-#include <endian.h>
+#if defined(__APPLE__)
+    #include <machine/endian.h>
+#else
+    #include <endian.h>
+#endif
 #define IEEEFP
 
 #endif /* vax */
diff -ruN a/tirpc/reentrant.h b/tirpc/reentrant.h
--- a/tirpc/reentrant.h	2024-07-22 13:10:59
+++ b/tirpc/reentrant.h	2024-08-30 23:15:01
@@ -36,7 +36,14 @@
  * These definitions are only guaranteed to be valid on Linux. 
  */
 
-#if defined(__linux__)
+#if defined(__APPLE__)
+    #define __APPLE_USE_RFC_3542
+    #define IPV6_PKTINFO IPV6_RECVPKTINFO
+    #define SOL_IP IPPROTO_IP
+    #define SOL_IPV6 IPPROTO_IPV6
+#endif
+
+#if defined(__linux__) || defined(__APPLE__)
 
 #include <pthread.h>
 
diff -ruN a/tirpc/rpc/rpcent.h b/tirpc/rpc/rpcent.h
--- a/tirpc/rpc/rpcent.h	2024-07-22 13:10:59
+++ b/tirpc/rpc/rpcent.h	2024-08-30 23:15:01
@@ -50,7 +50,7 @@
 
 /* These are defined in /usr/include/rpc/netdb.h, unless we are using
    the C library without RPC support. */
-#if defined(__UCLIBC__) && !defined(__UCLIBC_HAS_RPC__) || !defined(__GLIBC__)
+#if defined(__UCLIBC__) && !defined(__UCLIBC_HAS_RPC__) || !defined(__GLIBC__) && !defined(__APPLE__)
 struct rpcent {
 	char	*r_name;	/* name of server for this rpc program */
 	char	**r_aliases;	/* alias list */
